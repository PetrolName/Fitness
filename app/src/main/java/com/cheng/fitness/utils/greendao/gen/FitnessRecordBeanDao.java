package com.cheng.fitness.utils.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.cheng.fitness.model.FitnessRecordBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "FITNESS_RECORD_BEAN".
*/
public class FitnessRecordBeanDao extends AbstractDao<FitnessRecordBean, Long> {

    public static final String TABLENAME = "FITNESS_RECORD_BEAN";

    /**
     * Properties of entity FitnessRecordBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Duration = new Property(1, String.class, "duration", false, "DURATION");
        public final static Property Time = new Property(2, String.class, "time", false, "TIME");
        public final static Property Distance = new Property(3, String.class, "distance", false, "DISTANCE");
        public final static Property Tire = new Property(4, String.class, "tire", false, "TIRE");
    };


    public FitnessRecordBeanDao(DaoConfig config) {
        super(config);
    }
    
    public FitnessRecordBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"FITNESS_RECORD_BEAN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"DURATION\" TEXT," + // 1: duration
                "\"TIME\" TEXT," + // 2: time
                "\"DISTANCE\" TEXT," + // 3: distance
                "\"TIRE\" TEXT);"); // 4: tire
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"FITNESS_RECORD_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, FitnessRecordBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String duration = entity.getDuration();
        if (duration != null) {
            stmt.bindString(2, duration);
        }
 
        String time = entity.getTime();
        if (time != null) {
            stmt.bindString(3, time);
        }
 
        String distance = entity.getDistance();
        if (distance != null) {
            stmt.bindString(4, distance);
        }
 
        String tire = entity.getTire();
        if (tire != null) {
            stmt.bindString(5, tire);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, FitnessRecordBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String duration = entity.getDuration();
        if (duration != null) {
            stmt.bindString(2, duration);
        }
 
        String time = entity.getTime();
        if (time != null) {
            stmt.bindString(3, time);
        }
 
        String distance = entity.getDistance();
        if (distance != null) {
            stmt.bindString(4, distance);
        }
 
        String tire = entity.getTire();
        if (tire != null) {
            stmt.bindString(5, tire);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public FitnessRecordBean readEntity(Cursor cursor, int offset) {
        FitnessRecordBean entity = new FitnessRecordBean( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // duration
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // time
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // distance
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4) // tire
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, FitnessRecordBean entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setDuration(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setTime(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setDistance(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setTire(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(FitnessRecordBean entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(FitnessRecordBean entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
