<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cheng.fitness">

    <application
        android:name=".common.base.BaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.activity.CourseDetailActivity" />
        <activity android:name=".views.activity.AboutActivity" />
        <activity android:name=".views.activity.SettingActivity" />
        <activity android:name=".views.activity.MineInfoActivity" />
        <activity android:name=".views.activity.MainActivity" />
        <activity android:name=".views.activity.RegisterActivity" />
        <activity android:name=".views.activity.CommunityDetailActivity" />
        <activity android:name=".views.activity.PublishActivity" />
    </application>

</manifest>